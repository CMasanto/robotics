classdef Set  % Union-Find data structure
    properties
        currentSize  % current number of elements in the set
        capacity  % maximum number of elements that can exist in the set
    end
    methods
        function [y] = find(x)
        end
        
        function [] = add(x)
        end
        
        function obj = Set()
            capacity = 1000
            currentSize = 
            
            % Creates the list
            obj.setList(1, maxListSize) = ObjectConstructor();
            for n = 1:maxSize
                obj.setList(n) = ObjectConstructor();
            end
        end
        
    end
end